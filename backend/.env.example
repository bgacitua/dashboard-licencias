# Dashboard Licencias - Variables de Entorno
# Copiar este archivo a .env y completar los valores

# ===== Base de Datos Licencias (IARRHH) =====
DB_SERVER=servidor
DB_USER=usuario
DB_PASSWORD=password
DB_NAME=IARRHH

# ===== Base de Datos Marcas (MorphoManager) =====
MARCAS_DB_SERVER=servidor
MARCAS_DB_USER=usuario
MARCAS_DB_PASSWORD=password
MARCAS_DB_NAME=MorphoManager

# ===== Configuración JWT =====
# IMPORTANTE: Cambiar por una clave segura en producción
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=cambiar-esta-clave-secreta-en-produccion-muy-larga
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480
