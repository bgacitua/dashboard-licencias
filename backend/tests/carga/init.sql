-- Esquemas que el backend espera. Las tablas las crea create_all al arrancar
-- (incluidas las de tickets.*, que es lo que se prueba).
CREATE SCHEMA IF NOT EXISTS app;
CREATE SCHEMA IF NOT EXISTS rh;
CREATE SCHEMA IF NOT EXISTS tickets;
